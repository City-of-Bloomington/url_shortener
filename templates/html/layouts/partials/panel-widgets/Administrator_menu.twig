<nav>
    <header><h2>{{ _('admin') }}</h2></header>
    {% if isAllowed('people', 'index') %}
    <a href="{{ uri('people.index') }}">{{ _(['person', 'people', 10]) }}</a>
    {% endif %}
    {% if isAllowed('users', 'index') %}
    <a href="{{ uri('users.index') }}">{{ _(['user', 'users', 10]) }}</a>
    {% endif %}

    
    {% if isAllowed('urls', 'index') %}
    <a href="{{ uri('urls.index') }}">{{ _(['urls', 'urls', 10]) }}</a>
    {% endif %}
</nav>
