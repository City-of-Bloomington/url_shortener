{#
 * @copyright 2023 City of Bloomington, Indiana
 * @license https://www.gnu.org/licenses/agpl.txt GNU/AGPL, see LICENSE
 *
 * @param array  urls          Search Results
 * @param array  usernames     List of all usernames
 *
 * -- Pagination parameters
 * @param int    total
 * @param int    itemsPerPage
 * @param int    currentPage
 *
 * -- Search Parameters
 * @param int    id
 * @param string username
 * @param string code
 * @param string original
 * @param string query
#}
{% extends "html/layouts/default.twig" %}
{% import  "html/macros/pagination.twig" as pagination %}

{% block content %}
<section>
    <header>
        <h1>Find URLs</h1>
    </header>
    <form method="get">
        <fieldset>
            <label for="query">Query</label>
            <input name="query" id="query" value="{{ query }}" placeholder="Search original or code"/>
            <button type="submit" class="search">Search</button>
        </fieldset>
    </form>
    <details {% if code or original or username %}open="true"{% endif %}>
        <summary>Advanced Search</summary>
        <form method="get">
            <fieldset>
                <div>
                    <label for="code">Code</label>
                    <input name="code" id="code" value="{{ code }}"
                                size="{{ CODE_LENGTH }}"
                        maxlength="{{ CODE_LENGTH }}" />
                </div>
                <div>
                    <label for="original">Original</label>
                    <input name="original" id="original" value="{{ original }}" />
                </div>
                <div>
                    <label  for="username">Username</label>
                    <select name="username" id="username"><option value=""></option>
                    {% for u in usernames %}
                        <option {% if u==username %}selected="selected"{% endif %}>{{ u }}</option>
                    {% endfor %}
                    </select>
                </div>
                <button type="submit" class="search">Search</button>
            </fieldset>
        </form>
    </details>

    {% if urls %}
    <table>
        <thead>
            <tr><th>Info</th>
                <th>Short URL</th>
                <th>Original URL</th>
            </tr>
        </thead>
        <tbody>
            {% for url in urls %}{% set uri = uri('urls.view', {'id':url.id}) %}
            <tr><td><a href="{{ uri('urls.view', {'id':url.id}) }}">{{ url.code }}</a></td>
                <td><a href="{{ url('urls.redirect', {'code':url.code}) }}">{{ url('urls.redirect', {'code':url.code}) }}</a></td>
                <td><a href="{{ url.original }}">{{ url.original }}</a></td>
            </tr>
            {% endfor %}
        </tbody>
    </table>
    {% else %}
        <p>No URLs found.</p>
    {% endif %}

    {# Pagination #}
    {% if total > itemsPerPage %}
        {{ pagination.pageLinks(total, itemsPerPage, currentPage) }}
    {% endif %}
</section>
{% endblock %}
